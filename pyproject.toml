[project]
name = "eth-graph-research"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "biosharp18", email = "rory.gao18@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "db-dtypes>=1.7.1",
    "google-cloud-bigquery>=3.42.2",
    "google-cloud-bigquery-storage>=2.39.0",
    "pandas>=3.0.5",
    "pyarrow>=25.0.0",
    "pydata-google-auth>=1.9.1",
]

[project.scripts]
eth-graph-research = "eth_graph_research:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
